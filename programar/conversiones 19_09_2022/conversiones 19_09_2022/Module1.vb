Module Module1

    Sub Main()
        'atento con single, integerm, double
        Dim numero As Integer = 10
        'porqué utilizamos el option strict on
        'pérdida de precisión
        'Dim raiz As Double = Math.Sqrt(numero)
        'Console.WriteLine("la raiz es: " & raiz)

        Dim x As Byte
        Dim y As Integer = 290
        'error desbordamiento

        Console.ReadLine()

    End Sub

End Module
